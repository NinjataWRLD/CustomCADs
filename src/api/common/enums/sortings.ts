export enum SortingDirection {
	Ascending,
	Descending,
}

export enum AccountSorting {
	CreationDate,
	Username,
	Email,
	Role,
}

export enum CartSorting {
	PurchaseDate,
	Total,
}

export enum OngoingOrderSorting {
	OrderDate,
	Alphabetical,
	OrderStatus,
	Delivery,
}

export enum CompletedOrderSorting {
	OrderDate,
	PurchaseDate,
	Alphabetical,
	Delivery,
}

export enum ProductSorting {
	UploadDate,
	Alphabetical,
	Status,
	Cost,
}

export enum ShipmentSorting {
	RequestDate,
	Country,
	City,
}
